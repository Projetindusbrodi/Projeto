<% if @order.errors.any? || @shopping_cart_item.errors.any? %>
alert("not valid.")
<% else %>
  $(".shopping_cart-text").html("<%= escape_javascript(render 'layouts/shopping_cart_text') %>")
<% end %>
